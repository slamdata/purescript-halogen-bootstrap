{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "generate-bootstrap": "cd dev && ./GenerateBootstrap.hs > ../src/Halogen/Themes/Bootstrap4.purs",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript-psa": "^0.5.0",
    "purescript": "^0.11.4",
    "rimraf": "^2.5.4"
  }
}
